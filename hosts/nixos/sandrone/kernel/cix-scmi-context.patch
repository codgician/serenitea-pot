Refresh the SCMI ACPI patch for Linux 6.18.52. Preserve the stable
kernel's transport device naming, early channel handle initialization,
channel cleanup on IDR allocation failure, and destruction by channel ID.
Only patch context changes; the CIX firmware-node conversion is unchanged.

--- a/0003-firmware-arm_scmi-add-acpi-support-to-SCMI.patch
+++ b/0003-firmware-arm_scmi-add-acpi-support-to-SCMI.patch
@@ -186,8 +186,8 @@
  		cinfo = idr_find(idr, SCMI_PROTOCOL_BASE);
  		if (unlikely(!cinfo)) /* Possible only if platform has no Rx */
  			return -EINVAL;
-@@ -2682,20 +2683,20 @@ static int scmi_chan_setup(struct scmi_info *info, struct device_node *of_node,
- 	snprintf(name, 32, "__scmi_transport_device_%s_%02X",
+@@ -2682,21 +2683,21 @@ static int scmi_chan_setup(struct scmi_info *info, struct device_node *of_node,
+ 	snprintf(name, sizeof(name), SCMI_TRANSPORT_DEVNAME_PREFIX "_%s_%02X",
  		 idx ? "rx" : "tx", prot_id);
  	/* Create a uniquely named, dedicated transport device for this chan */
 -	tdev = scmi_device_create(of_node, info->dev, prot_id, name);
@@ -203,6 +203,7 @@
  
  	cinfo->id = prot_id;
  	cinfo->dev = &tdev->dev;
+ 	cinfo->handle = &info->handle;
  	ret = info->desc->ops->chan_setup(cinfo, info->dev, tx);
  	if (ret) {
 -		of_node_put(of_node);
@@ -210,10 +211,11 @@
  		scmi_device_destroy(info->dev, prot_id, name);
  		devm_kfree(info->dev, cinfo);
  		return ret;
-@@ -2718,7 +2719,7 @@ static int scmi_chan_setup(struct scmi_info *info, struct device_node *of_node,
+@@ -2718,8 +2719,8 @@ static int scmi_chan_setup(struct scmi_info *info, struct device_node *of_node,
  			"unable to allocate SCMI idr slot err %d\n", ret);
  		/* Destroy channel and device only if created by this call. */
  		if (tdev) {
+ 			info->desc->ops->chan_free(prot_id, cinfo, idr);
 -			of_node_put(of_node);
 +			fwnode_handle_put(fwnode);
  			scmi_device_destroy(info->dev, prot_id, name);
@@ -258,13 +260,13 @@
  			continue;
  
  		if (!FIELD_FIT(MSG_PROTOCOL_ID_MASK, prot_id))
-@@ -2803,7 +2804,7 @@ static int scmi_chan_destroy(int id, void *p, void *idr)
+@@ -2803,7 +2804,7 @@ static int scmi_chan_destroy(int id, void *p, void *data)
  		struct scmi_info *info = handle_to_scmi_info(cinfo->handle);
  		struct scmi_device *sdev = to_scmi_dev(cinfo->dev);
  
 -		of_node_put(cinfo->dev->of_node);
 +		fwnode_handle_put(cinfo->dev->fwnode);
- 		scmi_device_destroy(info->dev, id, sdev->name);
+ 		scmi_device_destroy(info->dev, cinfo->id, sdev->name);
  		cinfo->dev = NULL;
  	}
 @@ -2864,7 +2865,7 @@ static int scmi_bus_notifier(struct notifier_block *nb,
